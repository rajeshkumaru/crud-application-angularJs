import sellerDetailsComponent from './SellerDetailsComponent';

describe('sellerDetailsComponent', () => {

  describe('SellerDetailCtrl', () => {
    // let ctrl;

    beforeEach(() => {
      const someClass = new sellerDetailsComponent()
    });

    it('should contain the value', () => {
      console.log(someClass);
    });
  });
});